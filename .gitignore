/.quarto/
**/.quarto/
.Rproj.user
.claude/

# Book ingestion intermediates (reproducible from source docx; not for git)
OLWTEF/ingest/_work/
OLWTEF/ingest/_media*/
OLWTEF/ingest/*.tmp.*
LTAV/ingest/_work/
LTAV/ingest/_media*/
LTAV/ingest/*.tmp.*
# Stray single-file renders in the source dir (book HTML belongs in docs/<book>/)
OLWTEF/[0-9]*.html
LTAV/[0-9]*.html
images/_originals/
Johan_podcast_dataset.xlsx
*.xlsx.bak

**/*.quarto_ipynb

# Editor/atomic-write temp files
*.tmp.*

# Geo-data cache for figure 8.1 (re-downloadable from FAO)
OLWTEF/ingest/_cache/
